use crate::clients::strava::StravaClient;

mod clients;

fn main() {
    println!("Hello, world!");

    // let Ok(strava_client) =
    //     StravaClient::new("asd".to_string(), "asd".to_string(), "asd".to_string())
    // else {
    //     return;
    // };
    //
    // print!(strava_client);
}
